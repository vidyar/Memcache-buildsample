language: python

python:
 - "2.6"
 
services:
 - memcached-1.4.15

install: pip install --use-mirrors -r requirements.txt

script:
 - python sample.py
 
notifications:
    email:
      - vamshi@shippable.com 
      - vidya@shippable.com
